package anonymous;

public class Road {
	public static void main(String[] args) {
		
		Car tank = new Car("M1", "Green", 1380000) { //익명클래스 <-  //익명클래스로 찍어낸 객체 즉, 익명클래스가 하나의 객체이다. ?

			int bomb = 40; //클래스 영역에 변수를 선언한거니깐 전역변수로 나온다.
			
			//메소드는 클래스 내부에 생성이 됨. 즉, 이 메소드 밖은 클래스이다. 하지만 클래스는 클래스명이 있어야 하는데, 보니깐 없음. 이런 것들을 익명 클래스라고 함.
			@Override
			void engineStart() {
				System.out.println(model + " 열쇠로 시동 켜기");
			}
			@Override
			void engineStop() {
				System.out.println(model + " 열쇠로 시동 끄기");
			}
			
			void shoot() {
				System.out.println("포단 쏘기!");
				bomb--;
			}
		};
		
		tank.engineStart();
		//(???)tank.shoot//부모에 자식 메서드를 넣어서 사용 불가. 업캐스팅이 일어남. 1. 오버라이딩해서 사용할 수 있다. 2. 자식에서 추가한 내용은 사용할 수 없음 / 형변환할 타입이 없어서 다운캐스팅을 할 방법이 없기 때문에 사용 불가
	}
}

//익명 클래스는 앞에 호출하고 있는 생성자의 클래스를 상속받고 있는 이름 없는 자식 클래스이다. 
//하나의 객체만을 위한 일회용 클래스이다. 즉, 그 앞에 호출하는 생성자를 통해 만드는 객체 딱 하나만을 위한 클래스이다. 
//그 때 만들어지는 객체는 익명 클래스 안에서 구현한 메소드의 내용을 가지게 된다.
//이렇게 만들어지는 객체를 익명구현객체 라고 한다.
//